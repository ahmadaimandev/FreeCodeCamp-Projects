JAVASCRIPT NOTE

--> variables points to specific memory address that stores a value.
--> declaring variables in javascript using keyword: (let)
--> variables naming follow specifics rules:
    1. names can include letters, numbers, dollar sign and underscore.
    2. cannot contain spaces and must not begin with numbers.

--> javascript have 7 primitive data types.
   1. strings ("text inside here") == strings are immutable. once its created they cannot be changed

--> print and view javascript output
    1. send information using console.log()

--> you can reassign or change the value of the variable when is declared with let keyword
     example:
     let programmer = "Naomi";
     programmer = "CamperChan";
--> Remember!
    1. when assigning new variable, do not use the let keyword again.